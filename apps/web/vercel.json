{
    
    "buildCommand": "npm run build --workspace=web",
    "devCommand": "npm run dev",
    "env": {
      "HUSKY_SKIP_INSTALL": "1"
    },
    "rewrites": [
      {
        "source": "/api/:path*",
        "destination": "https://salesedge-ai-production.up.railway.app/:path*"
      }
    ]
  }
  
